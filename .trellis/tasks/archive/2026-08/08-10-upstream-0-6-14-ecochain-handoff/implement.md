# Implementation Plan

1. Treat upstream `c8e327a9` as the implementation baseline and inventory each fork delta as upstream-covered, handoff-required, compatible legacy behavior, or retired behavior; do not restore history by file or commit wholesale.
2. Reapply the minimal compatible distribution identity: `@ecochain/trellis`, `@ecochain/trellis-core`, package version `0.6.14`, the team GitLab repository metadata, and the default `private` release remote.
3. Implement the independent Ecochain Git tag namespace `ecochain-v<version>`, including `ecochain-v0.6.14` for this release. Keep upstream `v<version>` tags untouched and make release/preflight validation bind the tag version to both Ecochain package manifests.
4. Change the release script to push only the exact current Ecochain tag to `private`; remove and prohibit `git push --tags`, then test that unrelated upstream/local tags are not included in the push plan.
5. Remove Aliyun npm registry configuration and fallback behavior from package metadata, workflows, docs, release/preflight scripts, and migration/install guidance. Verify repository-owned npm commands do not force a registry and honor default/user npm configuration. Do not mutate the user's npm configuration; report it separately if it still selects Aliyun.
6. Restrict any retained GitHub publish workflow to explicit manual recovery. Accept only `ecochain-v*`, assert the checked-out package names are `@ecochain/trellis` and `@ecochain/trellis-core` before publish, and verify no push, tag, schedule, or release event can trigger publication.
7. Treat npmjs `@ecochain` scope ownership and a valid `NPM_TOKEN` (or explicitly approved equivalent) as external, fail-closed release prerequisites. Record their verification separately; do not simulate credentials or count local workflow/tests as proof that publication authority exists.
8. Reuse upstream private Git template-registry and spec-discovery behavior instead of reintroducing equivalent legacy implementations.
9. Add structured Task Basis parsing, compatibility behavior, and classified lifecycle gates with tests; do not restore the global plan/intent gate or `session-gate.json` truth source.
10. Align workflow, brainstorming, before-dev, and check templates with natural-language routing, optional TDD, and JSONL manifest semantics. Restore the Dazz constraint voice as a presentation layer over classified governance in the common start, brainstorm, before-dev, and workflow truth sources; do not restore blanket Product Intent semantics or deleted Codex skill sources.
11. Make creator/joiner bootstrap generation perform team-spec fit/gap review and remove fixed sub-agent/command-chain claims.
12. Add the shared verification profile/status/evidence contract while keeping GitLab, Codeup, Sonar, and governance-source execution out of the repository.
13. Render and inspect platform outputs, then update regression/integration tests. Add durable checks that the Dazz contract reaches generated Claude/Codex and fresh-init output, remains limited to Trellis-owned constraints, and does not block readonly/operational work or recreate blanket Product Intent. Search the complete change for Aliyun registry references, stale official package names in actionable Ecochain install/upgrade guidance, unintended GitHub publication triggers, use of upstream `v<version>` as an Ecochain release tag, and broad tag pushes.
14. Run focused tests followed by test, typecheck, lint, build, and format checks. Dry-run business development, bugfix/maintenance, Dazz teaching, and check paths in fresh projects. Review the complete diff against the disposition matrix and record any remaining `ai-governance` work separately. Keep npmjs authority/credential verification explicitly external to the local implementation result.
